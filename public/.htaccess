Options -Indexes
RewriteEngine On

# Route all requests to the front controller when public/ is the document root.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [L]
